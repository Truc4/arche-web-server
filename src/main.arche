main :: proc() {
  arche_argv(1)(portstr:);
  port := atoi(portstr);
  arche_argv(2)(root:);

  net.listen(port)(srv:);
  print("arche-web-server listening\n");

  for (running := true; running;) {
    net.accept(srv)(conn:);
    serve(conn, root);
  }
}
