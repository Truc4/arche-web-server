#import {os net fmt server parse}

main :: proc() {
  os.os_argv(1)(portstr:);
  port := parse.atoi(portstr);
  os.os_argv(2)(root:);

  net.net_listen(port)(srv:);
  fmt.print("arche-web-server listening\n");

  for (;;) {
    net.net_accept(srv)(conn:);
    server.serve(conn, root);
  }
}
